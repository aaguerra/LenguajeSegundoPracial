<%= bootstrap_form_for(@quiz, label_col: "col-sm-2", control_col: "col-sm-10") do |f|%>
  <%= f.text_field :pin, label: 'Ingrese el PIN del QUIZ'%>
  <%= f.submit "Iniciar quiz",class: "btn btn-success"%>
<%end%>
<%if @error == 1%>
  <%=render partial: 'layouts/error' %>
<%end%>
